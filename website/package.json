{
  "name": "reply-ninja",
  "version": "1.0.0",
  "description": "comment system for static website",
  "author": "Saurabh Chandra Rai",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_ENV=development nodemon server/index.js --watch server",
    "build": "nuxt build",
    "start": "cross-env NODE_ENV=production node server/index.js",
    "generate": "nuxt generate"
  },
  "dependencies": {
    "nuxt": "^2.0.0",
    "cross-env": "^5.2.0",
    "express": "^4.16.4"
  },
  "devDependencies": {
    "@nuxtjs/vuetify": "^1.8.5",
    "nodemon": "^1.18.9"
  }
}
